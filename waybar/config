{
    "layer": "top",
    "position": "top",
    "height": 34,
    "spacing": 4, 
    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["clock"],
    "modules-right": ["cpu", "memory", "custom/gpu", "pulseaudio", "tray"],

    "hyprland/workspaces": {
        "on-click": "activate",
        "format": "{name}",
        "all-outputs": true
    },

  
    "pulseaudio": {
        // Добавили пробел после {icon}, чтобы не нализало на цифры
        "format": "{icon}  {volume}%", 
        "format-muted": "  0%",
        "format-icons": {
            "headphone": "",
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },

    "custom/gpu": {
        "exec": "nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits",
        "interval": 2,
        "format": "    {}%", // Иконка видеокарты (Nerd Font)
        "tooltip": false
    },

    "memory": {
        "interval": 2,
        "format": "  {used:0.1f}G"
    },

    "cpu": {
        "interval": 2,
        "format": "  {usage}%"
    }
}
